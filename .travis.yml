language: node_js
node_js:
  - '8'
cache: yarn
install:
  - yarn install
script:
  - yarn lint

before_deploy:
  - yarn build

deploy:
  provider: releases
  api_key: $GH_TOKEN
  file_glob: true
  file: dist/*
  skip_cleanup: true
  overwrite: true
  on:
    branch: fix-travis-ci
